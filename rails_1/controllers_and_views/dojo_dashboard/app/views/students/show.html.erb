<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>All the Dojos</title>
		<style media="screen">
			.link{
				display: inline-block;
				margin-top: 50px;
				color: blue;
			}

			.error{
				color: red;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<h1><%= @dojo.branch %>'s <%= @student.first_name%> <%= @student.last_name %></h1>

			<div class="info">
				<p>Email: <%= @student.email%></p>
			</div>

			<h2>Cohort</h2>

			<table>
				<thead>
					<tr>
						<th>First Name</th>
						<th>Last Name</th>
						<th>Email</th>
					</tr>
				</thead>
				<tbody>
					<% @students.each do |student| %>
					<tr>
						<td><%= student.first_name %></td>
						<td><%= student.last_name %></td>
						<td><%= student.email %></td>
					</tr>
					<% end %>
				</tbody>
			</table>

			<a href="/dojos/">Go Home</a>
			<a href="/dojos/<%=@dojo.id%>/students">Go to Student's Dojo</a>
			<a href="/dojos/<%=@dojo.id%>/students/<%=@student.id%>/edit">Edit</a>
		</div>
	</body>
</html>
